<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.app.baseonandroidonlinemall">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppBaseTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.fragment.product.detail.ProductDetailActivity">
        </activity>
        <activity android:name=".ui.fragment.user.login.LoginActivity">
        </activity>
        <activity android:name=".ui.fragment.order.OrderActivity">
        </activity>
        <activity android:name=".ui.fragment.product.list.ProductListActivity">
        </activity>
        <activity android:name=".ui.fragment.order.orderPay.OrderPayActivity">
        </activity>
        <activity android:name=".ui.fragment.order.list.OrderListActivity">
        </activity>
    </application>

</manifest>